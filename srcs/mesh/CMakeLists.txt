FIND_PACKAGE(CGAL REQUIRED)
IF(CGAL_FOUND)
    MESSAGE(STATUS "Found CGAL: " ${CGAL_DIR})
ENDIF()

SET(LIBMESHSRCS
./Mesh.cpp ./Mesh2D.cpp ./Mesh3D.cpp
./Mesh.hpp ./Mesh.inl ./Node.hpp)

ADD_LIBRARY(Mesh SHARED ${LIBMESHSRCS})
GENERATE_EXPORT_HEADER(Mesh
                       EXPORT_MACRO_NAME MESH_API
					   EXPORT_FILE_NAME Mesh_export.h)
TARGET_INCLUDE_DIRECTORIES(Mesh SYSTEM
                           PUBLIC ${CMAKE_CURRENT_BINARY_DIR} ${JSON_INCLUDE_DIRS}
                           PRIVATE ${GMSH_INCLUDE_DIRS})
TARGET_LINK_LIBRARIES(Mesh PRIVATE CGAL::CGAL ${GMSH_LIBRARIES})
